# DurableExecutionConfig Contract Schema
# Timeouts and durability knobs for workflow/session execution

$schema: "https://json-schema.org/draft/2020-12/schema"

type: object
required: [persistence_backend]
properties:
  start_to_close_timeout_seconds:
    type: integer
    minimum: 1
  schedule_to_start_timeout_seconds:
    type: integer
    minimum: 1
  heartbeat_timeout_seconds:
    type: integer
    minimum: 1
  checkpoint_on_every_transition:
    type: boolean
    default: true
  persistence_backend:
    type: string
    enum: [sqlite, postgres]
    default: postgres
    description: "Storage backend for durable workflow/session state (required)"
